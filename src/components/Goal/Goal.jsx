import "./Goal.css";

const Goal = () => {
  return (
    <div>
      <p className="goal">
        <span>Goal:</span> Turn all the lights off. Clicking a light toggles it
        and its neighbours.
      </p>
    </div>
  );
};

export default Goal;
